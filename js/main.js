var input=document.getElementById("input"); //input and output panel
var operator=document.querySelectorAll(".operators"); //operator buttons
var number=document.querySelectorAll(".numbers"); //number buttons
//console.log(number);
var result=document.getElementById("result"); //equal button
var clear=document.getElementById("clear"); //clear button
resultDisplayed=false; //flag to keep an eye on what output is displayed
//adding click handlers event
for(var i=0;i<number.length;i++){
number[i].addEventListener("click",function(e){
    
})
}

